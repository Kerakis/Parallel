name: Update Card Names Catalog
on:
  schedule:
    - cron: 0 0 * * SUN
     # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  update-cards:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: Fetch API Data
        uses: JamesIves/fetch-api-data-action@v2.2.0
        with:
          endpoint: https://api.scryfall.com/cards/e9d5aee0-5963-41db-a22b-cfea40a967a3
          configuration: '{ "method": "GET" }'
          save-location: assets
          save-name: test
